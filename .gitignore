node_modules/
dist/
.env
.env.local
*.local
panel/node_modules/
panel/dist/
panel/.vercel/
*.log

# Panel snapshot'ları — TÜRETİLMİŞ veri, kaynak değil.
# araclar/panelSnapshot.ts DB'den yeniden üretiyor. 9.8 MB (30 bin bayi) ve her
# tazelemede tamamı değişiyor → versiyonlamak depoyu hızla şişirir.
panel/public/api/

.vercel
# Vercel CLI `.env*` eklemişti — `.env.example` ŞABLONU repoda kalmalı
# (yeni kurulumda hangi değişkenler gerektiğini gösteren tek kaynak).
.env*
!.env.example

# gecici olcum/test betikleri (AI arac ciktilari)
.olc.ts
.mutOlc.ts
.test.ts

# Gecici DB sorgu betikleri ve ciktilari (2026-08-25).
# Bu klasorde nokta ile baslayan .mjs/.json/.txt dosyalari AI oturumlarinda uretilen
# tek kullanimlik araclardir: canli Logo/ParkB2B'ye baglanir, gercek fis/bayi verisi
# icerir, bazen API anahtari tutar. REPO PUBLIC -> asla commit edilmemeli.
.*.mjs
.*.json
.*.txt
_*.txt
_*.mjs
.k
